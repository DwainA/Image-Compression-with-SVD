""" 
This module will take in a Image URL and then display a new Image after 
running it through the mathematical process of singular value decomposition
"""

#Reading Image URL
import urllib
import cv2
#Main Libraries for SVD Calculatoes
import numpy as np
from numpy import linalg as compute
from PIL import Image
#Useful for me
import math


def convert_url_to_matrix(image_url):
    """
    Returns a symmetric 2D Array, otherwise known as a matrix, given an image url (JavaScript Front End)

    This function works assumming imageurl is a valid image url.
    """
    try:
        response = urllib.request.urlopen(image_url)
        img_array = cv2.imdecode(bytearray(response.read()), cv2.IMREAD_COLOR)
        return img_array
    except:
        print("Error")


def matrixCalculations():
    """
    Placeholder for SVD stuff 
    """
    pass